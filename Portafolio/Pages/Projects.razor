@page "/projects"
@inject ProjectService ProjectService

<PageTitle>Projects | Hugo Palafox</PageTitle>

<SectionContainer>
    <PageHeader Eyebrow="Projects" Title="Engineering Project Portfolio" Description="Case studies across robotics, controls, embedded edge, and OT-IT architecture." />
    <div class="grid grid-3">
        @foreach (var project in ProjectsList)
        {
            <ProjectCard Project="@project" @key="project.Id" />
        }
    </div>
</SectionContainer>

@code {
    private IReadOnlyList<Project> ProjectsList { get; set; } = Array.Empty<Project>();

    protected override void OnInitialized()
    {
        ProjectsList = ProjectService.GetAllProjects();
    }
}
